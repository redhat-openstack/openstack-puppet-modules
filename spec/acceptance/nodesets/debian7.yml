HOSTS:
  debian-server:
    roles:
      - master
    platform: debian-7-amd64
    image: debian:7
    hypervisor: docker
    docker_image_commands:
      - 'apt-get install -y wget'
CONFIG: 
  type: foss
