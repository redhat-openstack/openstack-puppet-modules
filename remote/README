puppet-remote

This Puppet module provides types and type providers for managing remote resources.
For example if you need to create MySQL database on a remote DB server.

DONE:
- MySQL resources (database, user, grant)

TO-DO:
- MongoDB resources (database, ...)
- OpenStack Keystone resources (services, users)
- PostgreSQL resources (database, users, ...)
