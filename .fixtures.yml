fixtures:
  repositories:
    stdlib:
      repo: "git://github.com/puppetlabs/puppetlabs-stdlib.git"
      ref: "2.6.0"
    lsb: "git://github.com/razorsedge/puppet-lsb.git"
  symlinks:
    snmp: "#{source_dir}"
